<div class="collection-header-wrp">
  <div class="collection-header">
    <h1 class="collection-header-title">{{ section.settings.title }}</h1>
    {%- if section.settings.collection_description -%}
      <p class="collection-header-desc">
        {{ section.settings.collection_description }}
      </p>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Collection Title/Desc",
  "class": "section section-collection",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Collection title"
    },
    {
      "type": "text",
      "id": "collection_description",
      "label": "Collection description"
    }
  ],
  "templates":["collection"],
  "presets": [
    {
      "name": "Collection Title/Desc"
    }
  ]
}
{% endschema %}
